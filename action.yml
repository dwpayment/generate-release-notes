name: "Generate release notes"
description: "generate release notes and comment to release issue"
author: "azutake"
inputs:
  base:
    required: true
    description: "base repository name"
  head:
    required: true
    description: "head repository name"
  release-issue-id:
    required: true
    description: "comment destination issue"
  #comment-template:
  #  required: false
  #  description: "template markdown file"
  token:
    description: "GITHUB_TOKEN or a repo scoped PAT."
    default: ${{ github.token }}
runs:
  using: "node16"
  main: "dist/index.js"
